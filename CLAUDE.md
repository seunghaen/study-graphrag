# CLAUDE.md

논문 그래프(OpenAlex) 위에 GraphRAG + 멀티에이전트를 AWS에 구축하는 8주 개인 프로젝트.
**목적은 결과물이 아니라 학습이다.**

**저장소는 이거 하나다.** 문서(`docs/` `journal/`)와 코드(`src/`, 2주차부터)가 같이 산다.
코드도 여기서 짜고 Claude의 도움을 받는다. **단 아래 작업 원칙을 지킬 것** —
특히 쿼리와 IAM 정책은 후보를 제시하고 사용자가 고른다(목표 1·2의 알맹이라서).

## 학습 목표 (모든 판단의 기준)

1. 온톨로지·그래프DB 설계 경험
2. AWS 환경에서 에이전트 직접 구현 (설계만 해본 적 있음)
3. AI 개발 도구 능숙하게 활용
4. RAG 설계·구현
5. 벡터DB와 그래프DB 차이를 알고 선택할 수 있기
6. 토큰 사용량을 고려한 설계

**제안이 이 6개 중 어디에도 기여하지 않으면 그렇다고 명시할 것.**

## 작업 원칙

- **코드보다 이해가 우선.** 사용자가 설명하지 못하면 배운 게 아니다.
- **분기점에서 멈추고 묻는다.** 코드를 짜주더라도, 설계 판단이 갈리는 지점에서는
  진행하지 말고 먼저 물을 것. 짠 다음에 설명하는 건 늦다.
- **쿼리와 IAM 정책은 후보를 2~3개 주고 사용자가 고른다.** (2026-08-12 변경.
  이전 규칙은 "사용자가 직접 쓴다"였음)
  - 문법 타이핑은 학습 대상이 아니다. **설계 공간을 보는 것**이 학습 대상이다.
  - 그래서 답 하나만 보여주지 말 것. 답이 하나면 그게 기준이 되어버려
    "빠진 게 있나 / 이게 맞는 모양인가"를 판단할 수 없다.
  - 사용자는 고르고 **이유를 댄다.** 이유가 안 나오면 넘어가지 말 것.
  - **검증 시점은 5주차.** 탐색 쿼리 하나를 백지에서 써보게 한다.
    술술 나오면 이 규칙을 풀고, 막히면 조회 쿼리는 직접 쓰는 쪽으로 되돌린다.
- **막히는 과정 자체가 학습이다.** 답을 먼저 주지 말 것. 질문과 힌트로 유도한다.
  단, **모르는 것과 막힌 것은 다르다.** 배경 지식이 아예 없어서 판단이 불가능한 상태면
  재료(개념·구조·선택지)는 준다. 결론은 사용자가 낸다.

## 대화 방식

- 한국어, **반말**, 간결하게
- 동의보다 정확함이 우선. 틀린 방향이면 바로 지적
- 사용자의 경험을 부풀리지 말 것 (백엔드 3년차, Java/Spring, AWS SAA 취득.
  그래프DB·온톨로지·RAG·에이전트는 처음. 도커·의존성 관리도 익숙하지 않음)
- AI스러운 표현, 모호한 용어 금지
- **한 번에 너무 많이 던지지 말 것.** 개념 설명이 길어지면 사용자가 지친다.
  하나씩, 예제 하나로 끝까지.

## 문서 지도

이 파일은 **매 세션 통째로 로드된다.** 그래서 여기엔 결론과 규칙만 둔다.
근거는 아래 문서에 있고, **해당 상황이 되면 반드시 먼저 읽는다.**

| 문서 | 언제 읽나 |
|---|---|
| [docs/decisions.md](docs/decisions.md) | 이미 정한 걸 다시 논쟁하려 할 때. 라우터·벡터 저장 위치·MCP 제외 등 |
| [docs/week0-data.md](docs/week0-data.md) | 데이터 소스를 의심하거나 바꾸려 할 때. API 응답 필드·요금 실측 |
| [docs/eval.md](docs/eval.md) | **4주차 진입 시 필수.** 베이스라인 3개, 평가 질문 자격, 토큰 설계 지점 |
| `journal/YYYY-MM-DD.md` | 세션 시작 시 **가장 최근 것 하나**를 읽어 어디서 끊겼는지 파악 |

문서가 커지면 지시가 묻힌다. **결론은 CLAUDE.md에, 근거는 docs에.** 이 원칙을 유지할 것.

## 저널 — 결론 말고 과정을 남긴다 (2026-08-04부터)

`docs/`는 무엇을 정했는지만 남기고 **어떻게 거기 도달했는지는 남기지 않는다.**
그런데 블로그·회고가 되는 건 그쪽이다. 그래서 `journal/`을 따로 둔다.

**작업이 한 덩어리 끝날 때마다 그날 파일에 이어 쓴다.** 사용자가 요청하지 않아도 먼저 챙길 것.
세션이 끊기면 사라지므로 미루지 말 것.

남길 것 — **틀렸던 생각을 반드시 포함한다.** 결론만 적으면 저널을 만든 의미가 없다.

- **잘못 알고 있었던 것** ← 제일 중요. 뭘 어떻게 잘못 알았고 왜 그렇게 생각했는지
- 막힌 지점과 푼 방법
- 결정과 그 이유 (상세 근거는 docs로, 여기엔 판단 흐름)
- 새로 알게 된 사실
- 다음에 할 것
- **블로그로 쓸 만한 것** — 글감 후보를 그때그때 적어둔다

주간 회고는 그 주 저널을 모아서 쓴다. 저널이 원재료다.

## 계획 (유동적 — 매주 갱신)

| 주차 | 내용 |
|------|------|
| 0 | Neo4j 손으로 만져보기 ✅ + 논문 데이터 실재 확인 ✅ |
| 1 | 온톨로지 모델링, 스키마 v1 (논문·저자·인용) ✅ |
| 2 | 데이터 파이프라인 (OpenAlex → Neo4j 적재) ✅ |
| 3 | **← 지금 여기.** AWS 인프라 — Neo4j를 AWS에서 닿게 + Bedrock 세팅 (휴가 사용) |
| 4 | 벡터 RAG 베이스라인 + 평가셋 + **토큰 계측** |
| 5 | 그래프 검색 + 벡터/그래프 비교 |
| 6 | **단일** 에이전트 (Bedrock) |
| 7 | **멀티에이전트** (라우터 구조) + 단일 대비 비교 |
| +1 | 버퍼 |

### 이 계획에는 버퍼가 없다

7주차까지 꽉 찼고 평가·정리가 +1주로 밀렸다. 즉 **버퍼가 정리에 먹힌다.**
하나는 못 끝날 가능성이 높으니 **잘라낼 순서를 미리 정해둔다.**

1. **7주차 멀티에이전트** — 제일 먼저 잘린다. 6주차 단일 에이전트가 남으면 목표 2는 채워진다
2. 6주차 단일 에이전트 — 여기까지 잘리면 목표 2가 죽으므로 최후 방어선
3. 1·2·4·5주차는 자르지 않는다 (목표 1·4·5·6의 뼈대)

**일정은 참고일 뿐 규칙이 아니다.**

- 계획과 다른 길이 더 나아 보이면 말할 것
- 범위가 늘어나면 늘어난다고 짚을 것
- 사용자가 계획에 집착하면 그것도 지적할 것

## 확정된 설계 (근거는 docs/decisions.md)

- **그래프DB: Neo4j** — 관계 프로퍼티, LLM의 Cypher 궁합, GraphRAG 생태계, 비용 0
- **벡터도 Neo4j 안에** — 임베딩은 노드 프로퍼티. 별도 벡터DB 없음
- **데이터 소스: OpenAlex 단독** — arXiv는 인용·저자ID가 없어 탈락
- **라우터가 고르는 건 DB가 아니라 검색 전략** — `벡터만 / 벡터→그래프 확장 / 그래프만`
- **공통 인용 확장의 허브 임계값 = 7** (2026-08-13). 인용자 8명 이상인 X는 징검다리로 안 쓴다.
  근거는 "10은 많고 5는 적어서" — **임의값이고 5주차에 조정한다.**
  남는 삼각형 50.9만 개(논문당 10.2개)로 평가셋 재료는 충분.
  조정 조건: B가 안 딸려오면 올리고, 딸려온 B가 주제를 벗어나면 내린다
- **멀티에이전트는 목표가 아니라 비교 대상** — 6주차 단일이 베이스라인, 7주차가 도전자
- **베이스라인은 셋** — LLM 단독 / 벡터 RAG / 그래프 RAG
- **토큰은 측정 항목이 아니라 설계 제약** — 4주차부터 계측
- **MCP 서버는 범위 밖.** 단 6~7주차 도구 계층은 나중에 MCP로 감쌀 수 있게 분리해 둘 것

### 2주차로 넘긴 제약 (전부 처리됨 2026-08-12)

- OpenAlex `search=`는 `filter=`의 10배 — 적재는 filter로만 ✅
- 초록은 `abstract_inverted_index` — 복원 필요 (20줄) ✅ `restore_abstract()`
- 분야 필터가 느슨함 ✅ — 토픽 ID 하나로 고정해서 해결.
  (분류가 실제로 지저분하다. "Geochemistry and Geologic Mapping"이 AI 서브필드에 들어가 있음)

### 3주차에 풀어야 할 것

로컬 도커 Neo4j는 Bedrock 에이전트가 접근하지 못한다. Lambda는 `localhost:7687`을 못 본다.
**6주차에 발견하면 그 주가 통째로 인프라 삽질이 된다.** EC2냐 관리형이냐는 3주차에 결정.
Neptune은 비용 때문에 배제.

### ✅ 비밀번호 분리 완료 (2026-08-13)

`ingest.py`는 이제 `os.environ`만 읽는다. 히스토리의 비밀번호도 `git filter-repo`로 제거.
**앞으로 비밀값을 다룰 때 지킬 것 세 가지** — 전부 이 작업에서 걸려 넘어진 것들이다.

1. **비밀값은 코드에만 새지 않는다.** 저널의 도커 실행 명령에도 그대로 적혀 있었다.
   진단할 때 `src/`만 보지 말고 **저장소 전체**를 훑을 것
2. **이 환경의 `grep`은 `.gitignore`를 존중한다** (셸 함수 + `--ignore-files`).
   비밀값을 찾는데 정작 `.env`가 스캔에서 빠진다. **비밀값 스캔은 `/usr/bin/grep`으로** 할 것
3. **비밀값을 검색하는 행위 자체가 그 문자열을 남긴다.** grep 명령이
   `.claude/settings.local.json` 권한 목록에 기록됐다 (gitignore라 무해했지만)

값의 출처는 실행 위치가 정한다 — 로컬은 `.env`, Lambda는 환경변수/Secrets Manager.
**코드는 어느 쪽인지 모른다.** 그래서 6주차에 실행 위치가 바뀌어도 `ingest.py`는 안 고친다.

## 도메인 선택 체크리스트

**도메인을 또 바꾸려 할 때 이 표로 먼저 검증한다.**

| # | 조건 | 왜 |
|---|---|---|
| 1 | **LLM이 모르는 데이터** | 이미 아는 데이터면 RAG 성능이 아니라 모델 사전지식을 재게 된다 |
| 2 | **무료·합법으로 구할 수 있음** | 유료 API·저작권 데이터에 의존하면 2주차에 막힌다 |
| 3 | **되돌아오는 관계** (`A → X ← B`) | 벡터 검색이 구조적으로 못 이기는 유일한 패턴 |
| 4 | **집계·완전성**이 필요한 질문 | 검색은 "찾은 것"을 주고 그래프는 "전부"를 보장한다 |
| 5 | **본인이 도메인을 앎** | 모르면 도메인 학습 + 온톨로지 학습을 동시에 하게 되어 8주에 불가능 |
| 6 | **흥미가 있음** | 8주 개인 프로젝트의 연료 |

| 후보 | 결과 | 사유 |
|---|---|---|
| 와인(부르고뉴) | 폐기 | 1·2·6 실패 |
| 패키지 의존성 | 폐기 | 5·6 실패 |
| **논문 (OpenAlex)** | **채택** | 1·2·3·4 통과. 5·6은 약함 |

5·6이 약한 것을 어떻게 다루는지는 [decisions.md #6](docs/decisions.md) 참고.
요약: **사용자의 흥미는 데이터가 아니라 시스템에 있다. 막히면 시스템 쪽으로 진도를 뺀다.**

## 진행 상황

> ### ⏸ 세션 재개 지점 (2026-08-13)
>
> **2주차 완전히 끝났다.** 적재 + 흔들렸던 전제 검증까지 완료.
> 그래프는 Neo4j에 있다 (`neo4j-study` 컨테이너, 214MB).
>
> **다음은 3주차 AWS 인프라.** 로컬 도커 Neo4j를 Bedrock이 닿을 수 있는 곳으로 옮기는 게 첫 문제.
> 맥락은 [journal/2026-08-13.md](journal/2026-08-13.md)에 있다.

**2주차 완료 (2026-08-13).** 다음은 3주차 AWS 인프라.

- Neo4j 도커 + `:play movies` 체험 완료. **버전 25 확인** — 벡터 인덱스(5.11+) 문제 없음
- API 실물 응답 확인 완료 — arXiv 탈락, OpenAlex 통과, Semantic Scholar는 익명 호출 429
- OpenAlex 요금 판정 — 무료 한도로 충분 (적재 5만 건에 $0.05어치)
- 스키마 v1 확정 — 노드 3개(Paper, Author, Topic), 관계 3개(AUTHORED, CITES, HAS_TOPIC). 상세는 [docs/schema-v1.md](docs/schema-v1.md)
- **`src/ingest.py` 완성 + 5만 건 적재 완료.** Stub 생성·Full 승격·멱등성·무결성 전부 확인
- **적재 범위: `type:article,topics.id:T10181,publication_year:>2019`** (NLP, 51,907건)
- **적재 병목은 Neo4j 쓰기가 아니라 API 왕복** — 1,000건 적재에 CPU 7%.
  `per_page`를 100→200(최대)으로 올려 절반으로 단축

### 적재된 그래프 (2026-08-12)

| | 개수 | |
|---|---|---|
| Paper:Full | 50,001 | +1은 승격 테스트로 넣은 2009년 논문. 필터 밖이니 헷갈리지 말 것 |
| Paper:Stub | 442,054 | |
| Author / Topic | 94,028 / 701 | |
| CITES | 1,144,378 | |
| AUTHORED / HAS_TOPIC | 133,491 / 138,584 | |
| 라벨 겹침 / 정체불명 노드 | **0 / 0** | 설계대로 |

- **참고문헌이 0개인 논문이 18,187건(36%)** — OpenAlex 인용 데이터가 불완전하다
- **초록 없는 Full 논문이 10,762건(21.5%)** — 4주차 임베딩에서 어떻게 다룰지 정해야 함

### 적재 파이프라인에서 나온 원칙 (앞으로도 적용)

- **MERGE는 그 자리에서 완전한 노드를 만들 수 있을 때만.**
  불완전한 노드를 만들어야 하면 `:Stub` 같은 라벨로 **불완전함을 표시**할 것.
  라벨 없는 껍데기는 버그인지 설계인지 구분이 안 된다
- **적재는 멱등해야 한다.** `ON CREATE`로만 채우면 변환 코드를 고칠 때마다 DB를 비워야 한다.
  단 `SET node = $props`(통째 치환)는 금지 — 4주차 `embedding`이 날아간다

### 적재 범위를 이걸로 고른 이유

후보 6개에서 `A → X ← B` 삼각형 밀도를 200건 표본으로 실측했다. NLP가 블록체인의 **8배**(169 vs 21).
[eval.md](docs/eval.md)대로 그래프 RAG와 벡터 RAG의 차이는 확장 단계 하나뿐이라,
데이터에 이 패턴이 없으면 5주차 결론이 "차이 없음"으로 끝난다.

NLP는 LLM이 제일 잘 아는 분야라 도메인 체크리스트 1번이 약하지만,
eval.md의 **질문 자격 3번("LLM이 그냥 맞히면 탈락")이 설계상 방어**한다.
실제 비용은 평가셋 만들 때 유명 논문 쌍이 많이 탈락하는 것 하나뿐.

### ✅ 흔들린 전제 — 검증 완료 (2026-08-13). 적재 범위 그대로 간다

범위 선택의 근거는 "NLP는 `A→X←B` 삼각형이 논문당 169개"였는데, 그 표본이 전체를 대표하지 않았다.

| | 논문당 참고문헌 |
|---|---|
| 표본 200건 | **121.9개** |
| 표본 1,000건 | 83.4개 |
| **전체 50,000건** | **22.9개** |

**교훈: 표본 크기를 바꿔가며 값이 수렴하는지 보지 않은 측정은 근거가 아니다.**
200 → 1,000에서 122 → 83으로 떨어지고 있었는데 그 신호를 못 읽었다.

**전체 그래프에서 다시 재보니 걱정은 기우였고, 진짜 문제는 다른 데 있었다.**
양이 아니라 **허브가 숫자를 지배하는 것**이 문제다. 아래 참조.

### 공통 인용 실측 — 허브가 숫자를 지배한다 (2026-08-13)

`A → X ← B`(공통 인용, co-citation) 삼각형을 전체 그래프에서 실측했다.
세는 법: X를 인용한 논문이 N편이면 그 X가 만드는 삼각형은 `N×(N-1)/2`. 구간별로 합산.

| X의 인용자 수 | X 개수 | 삼각형 수 | 비중 |
|---|---|---|---|
| 2~5 | 116,815 | 328,094 | 0.9% |
| 6~10 | 17,812 | 440,043 | 1.2% |
| 11~30 | 10,190 | 1,425,434 | 3.9% |
| 31~100 | 2,284 | 3,079,694 | 8.4% |
| 101~500 | 396 | 7,435,780 | 20.3% |
| **501+** | **38** | **23,990,347** | **65.4%** |
| 합계 | 147,535 | 36,699,392 | |

**논문 38개(0.03%)가 전체 삼각형의 65%를 만든다.** 101명 이상(434개)까지 넓히면 85.6%.
총합을 세는 건 사실상 **NLP 고전 몇 편을 세는 것**이라 아무 정보가 없다.

인용자 분포(전체 45.4만 X): 평균 2.5, **중앙값 1**, p90 4, p99 22, 최대 2,885.
중앙값이 1이라 인용당한 논문 절반은 징검다리가 아예 안 된다.

**핵심: 공통 인용이 신호인 건 X가 흔하지 않을 때뿐이다.**
"둘 다 Attention을 인용" = "둘 다 NLP 논문임" — 정보 0. 흔한 걸 공유하는 건 신호가 아니다.

### 다음에 할 것

1. **3주차 AWS 인프라** ← 다음 차례. 로컬 도커 Neo4j는 Bedrock에서 못 닿는다.
   214MB 그래프를 EC2로 어떻게 옮길지가 3주차 첫 문제
2. 초록 없는 21.5%를 4주차에 어떻게 다룰지 (제목 대체 / 제외)
3. OpenAlex API 키 발급 — 적재가 끝났으니 급하지 않음
